# Cilium should be in kube-system.
AVD-KSV-0037

# Dont need S3 bucket logging.
AVD-AWS-0089

# AWS encryption is good enough.
AVD-AWS-0132

# Terraform cloud workspaces do need unrestricted S3 IAM policies.
AVD-AWS-0345

# Attach policy directly to user, policies are user specific
AVD-AWS-0143

# ESO needs read access to sync secrets cross-namespace (least-privilege via resourceNames)
AVD-KSV-0113

# OAuth rotation job needs patch access to deployments for rollout restart (scoped to specific resourceNames)
AVD-KSV-0048

# CronJobs use bitnami/kubectl:latest - acceptable for maintenance jobs
AVD-KSV-0013
AVD-KSV-0125
