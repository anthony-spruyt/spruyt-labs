apiVersion: "cilium.io/v2"
kind: CiliumClusterwideNetworkPolicy
metadata:
  name: allow-all
spec:
  endpointSelector: {}  # Selects all endpoints
  ingress:
    - fromEntities:
        - all
  egress:
    - toEntities:
        - all