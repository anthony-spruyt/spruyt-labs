---
# yaml-language-server: $schema=https://datreeio.github.io/CRDs-catalog/traefik.io/ingressroute_v1alpha1.json
# Documentation: https://doc.traefik.io/traefik/reference/routing-configuration/kubernetes/crd/http/ingressroute/
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ingress-routes-lan-https
  namespace: redisinsight
  annotations:
    cert-manager.io/cluster-issuer: ${CLUSTER_ISSUER}
    external-dns.alpha.kubernetes.io/hostname: redisinsight.lan.${EXTERNAL_DOMAIN}
spec:
  entryPoints:
    - websecure
  routes:
    # Authentik outpost path - required for SSO auth flow redirects
    - kind: Rule
      match: Host(`redisinsight.lan.${EXTERNAL_DOMAIN}`) && PathPrefix(`/outpost.goauthentik.io/`)
      middlewares:
        - name: lan-ip-whitelist
        - name: compress
      services:
        - name: ak-outpost-redisinsight-outpost
          passHostHeader: true
          port: 9000
    # Main route with SSO forward-auth
    - kind: Rule
      match: Host(`redisinsight.lan.${EXTERNAL_DOMAIN}`)
      middlewares:
        - name: authentik-forward-auth
        - name: lan-ip-whitelist
        - name: compress
      services:
        - name: redisinsight-svc
          namespace: redisinsight
          passHostHeader: true
          port: 5540
  tls:
    secretName: "redisinsight-lan-${EXTERNAL_DOMAIN/./-}-tls"
