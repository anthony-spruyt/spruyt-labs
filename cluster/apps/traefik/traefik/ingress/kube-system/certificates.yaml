---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/cert-manager.io/certificate_v1.json
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: "hubble-internal-${EXTERNAL_DOMAIN/./-}-tls-kube-system"
  namespace: kube-system
spec:
  secretName: "hubble-internal-${EXTERNAL_DOMAIN/./-}-tls"
  issuerRef:
    name: ${CLUSTER_ISSUER}
    kind: ClusterIssuer
  commonName: "hubble.lan.${EXTERNAL_DOMAIN}"
  dnsNames:
    - "hubble.lan.${EXTERNAL_DOMAIN}"
