---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/cert-manager.io/certificate_v1.json
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: "headlamp-lan-${EXTERNAL_DOMAIN/./-}"
  namespace: headlamp-system
spec:
  secretName: "headlamp-lan-${EXTERNAL_DOMAIN/./-}-tls"
  issuerRef:
    name: ${CLUSTER_ISSUER}
    kind: ClusterIssuer
  dnsNames:
    - "headlamp.lan.${EXTERNAL_DOMAIN}"
