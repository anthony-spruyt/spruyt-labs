---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/cert-manager.io/certificate_v1.json
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: "openclaw-${EXTERNAL_DOMAIN/./-}"
  namespace: openclaw
spec:
  secretName: "openclaw-${EXTERNAL_DOMAIN/./-}-tls"
  issuerRef:
    name: ${CLUSTER_ISSUER}
    kind: ClusterIssuer
  dnsNames:
    - "openclaw.${EXTERNAL_DOMAIN}"
