---
# yaml-language-server: $schema=https://datreeio.github.io/CRDs-catalog/traefik.io/ingressroute_v1alpha1.json
# Documentation: https://doc.traefik.io/traefik/reference/routing-configuration/kubernetes/crd/http/ingressroute/
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ingress-routes-wan-https
  namespace: openclaw
  annotations:
    cert-manager.io/cluster-issuer: ${CLUSTER_ISSUER}
    external-dns.alpha.kubernetes.io/hostname: openclaw.${EXTERNAL_DOMAIN}
spec:
  entryPoints:
    - websecure
  routes:
    # Authentik outpost path - required for SSO auth flow redirects
    # Uses standalone outpost deployed to local namespace by Authentik
    - kind: Rule
      match: Host(`openclaw.${EXTERNAL_DOMAIN}`) && PathPrefix(`/outpost.goauthentik.io/`)
      middlewares:
        - name: compress
      services:
        - name: ak-outpost-openclaw-outpost
          passHostHeader: true
          port: 9000
    # Main OpenClaw route with SSO forward-auth
    - kind: Rule
      match: Host(`openclaw.${EXTERNAL_DOMAIN}`)
      middlewares:
        - name: authentik-forward-auth
        - name: compress
      services:
        - name: openclaw
          namespace: openclaw
          passHostHeader: true
          port: 18789
  tls:
    secretName: "openclaw-${EXTERNAL_DOMAIN/./-}-tls"
