---
# Default values: https://github.com/bjw-s-labs/helm-charts/blob/main/charts/library/common/values.yaml
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/refs/heads/main/charts/library/common/values.schema.json
defaultPodOptions:
  priorityClassName: low-priority
  automountServiceAccountToken: false
  securityContext:
    runAsNonRoot: true
    runAsUser: 65534
    runAsGroup: 65534
    fsGroup: 65534
    seccompProfile:
      type: RuntimeDefault
controllers:
  github-mcp-server:
    strategy: Recreate
    annotations:
      configmap.reloader.stakater.com/reload: "github-mcp-caddyfile"
    containers:
      app:
        image:
          repository: ghcr.io/github/github-mcp-server
          tag: v0.33.0@sha256:0371f3ca3128f4fa516672838f8eceaeb3c8f32e0c58725cd5737432588091ff
          pullPolicy: IfNotPresent
        args:
          - "http"
          - "--port"
          - "8083"
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop:
              - ALL
        resources:
          requests:
            cpu: 10m
            memory: 64Mi
          limits:
            memory: 256Mi
        probes:
          liveness:
            enabled: true
            custom: true
            spec:
              tcpSocket:
                port: 8083
              initialDelaySeconds: 10
              periodSeconds: 30
              timeoutSeconds: 3
              failureThreshold: 3
          readiness:
            enabled: true
            custom: true
            spec:
              tcpSocket:
                port: 8083
              initialDelaySeconds: 5
              periodSeconds: 10
              timeoutSeconds: 3
              failureThreshold: 3
          startup:
            enabled: true
            custom: true
            spec:
              tcpSocket:
                port: 8083
              failureThreshold: 30
              periodSeconds: 5
      auth-proxy:
        image:
          repository: caddy
          tag: 2.11.2-alpine@sha256:834468128c7696cec0ceea6172f7d692daf645ae51983ca76e39da54a97c570d
          pullPolicy: IfNotPresent
        args:
          - "caddy"
          - "run"
          - "--config"
          - "/etc/caddy/Caddyfile"
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop:
              - ALL
            add:
              - NET_BIND_SERVICE
        resources:
          requests:
            cpu: 5m
            memory: 16Mi
          limits:
            memory: 64Mi
        probes:
          liveness:
            enabled: true
            custom: true
            spec:
              httpGet:
                path: /healthz
                port: 8082
              periodSeconds: 30
              timeoutSeconds: 3
              failureThreshold: 3
          readiness:
            enabled: true
            custom: true
            spec:
              httpGet:
                path: /healthz
                port: 8082
              periodSeconds: 10
              timeoutSeconds: 3
              failureThreshold: 3
          startup:
            enabled: true
            custom: true
            spec:
              httpGet:
                path: /healthz
                port: 8082
              failureThreshold: 30
              periodSeconds: 5
service:
  app:
    controller: github-mcp-server
    ports:
      http:
        port: 8082
persistence:
  caddyfile:
    type: configMap
    name: github-mcp-caddyfile
    advancedMounts:
      github-mcp-server:
        auth-proxy:
          - path: /etc/caddy/Caddyfile
            subPath: Caddyfile
            readOnly: true
  github-pat:
    type: secret
    name: github-mcp-credentials
    advancedMounts:
      github-mcp-server:
        auth-proxy:
          - path: /etc/secrets
            readOnly: true
  caddy-data:
    type: emptyDir
    medium: Memory
    sizeLimit: 10Mi
    advancedMounts:
      github-mcp-server:
        auth-proxy:
          - path: /data
          - path: /config
