---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./velero-secret.sops.yaml
  - ./release.yaml
  - ./network-policies.yaml
configMapGenerator:
  - name: velero-values
    namespace: velero
    files:
      - values.yaml
configurations:
  - ./kustomizeconfig.yaml
