---
# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/velero.io/schedule_v1.json
# Velero Documentation: https://velero.io/docs/v1.17/api-types/schedule/
apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: spruyt-labs-cluster-backup
  namespace: velero
spec:
  paused: true # Temporarily paused during ms-01-2 RMA (issue #236)
  schedule: "0 17 * * *" # At 03:00 daily Melbourne (UTC+10)
  skipImmediately: false
  useOwnerReferencesInBackup: true
  template:
    storageLocation: aws-primary
    snapshotMoveData: true
    ttl: 600h0m0s # 25 days
    metadata:
      labels:
        velero.io/backup-type: "cluster"
        velero.io/schedule-name: "spruyt-labs-cluster-backup"
