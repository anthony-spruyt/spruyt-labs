---
apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: dual-stack-ip-pool
  namespace: metallb-system
spec:
  addresses:
    - "${NETWORK_LB_CIDR_IP6}"
    - "${NETWORK_LB_CIDR_IP4}"
