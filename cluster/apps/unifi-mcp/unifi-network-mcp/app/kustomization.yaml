---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./unifi-network-mcp-secrets.sops.yaml
  - ./release.yaml
  - ./network-policies.yaml
  - ./vpa.yaml
configMapGenerator:
  - name: unifi-network-mcp-values
    namespace: unifi-mcp
    files:
      - values.yaml
configurations:
  - ./kustomizeconfig.yaml
