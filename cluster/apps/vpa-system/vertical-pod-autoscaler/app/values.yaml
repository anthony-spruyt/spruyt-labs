# Default values for vertical-pod-autoscaler
# Chart: https://github.com/kubernetes/autoscaler/tree/master/charts/vertical-pod-autoscaler

admissionController:
  replicas: 1
  priorityClassName: high-priority
  certGen:
    enabled: false
  certManager:
    enabled: true
    createSelfSignedIssuer:
      enabled: true
  # minAvailable: 1 on a single replica blocks node drains, breaking Talos upgrades.
  podDisruptionBudget:
    enabled: false
  resources:
    requests:
      cpu: 50m
      memory: 256Mi
    limits:
      memory: 512Mi

recommender:
  replicas: 1
  priorityClassName: high-priority
  extraArgs:
    - --pod-recommendation-min-cpu-millicores=1
    - --pod-recommendation-min-memory-mb=10
    - --storage=prometheus
    - --prometheus-address=http://vmsingle-victoria-metrics-k8s-stack.observability.svc:8428
    - --prometheus-cadvisor-job-name=kubelet
    - --history-length=14d
    - --container-pod-name-label=pod
    - --container-name-label=container
    - --pod-namespace-label=namespace
    - --pod-name-label=pod
    - --metric-for-pod-labels=kube_pod_labels{job="kube-state-metrics"}[8d]
  podDisruptionBudget:
    enabled: false
  resources:
    requests:
      cpu: 50m
      memory: 512Mi
    limits:
      memory: 1024Mi

updater:
  replicas: 1
  priorityClassName: high-priority
  podDisruptionBudget:
    enabled: false
  resources:
    requests:
      cpu: 50m
      memory: 512Mi
    limits:
      memory: 1024Mi
