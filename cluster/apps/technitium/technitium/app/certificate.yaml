---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/cert-manager.io/certificate_v1.json
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: "dns-lan-${EXTERNAL_DOMAIN/./-}"
  namespace: technitium
spec:
  secretName: "dns-lan-${EXTERNAL_DOMAIN/./-}-tls"
  issuerRef:
    name: ${CLUSTER_ISSUER}
    kind: ClusterIssuer
  dnsNames:
    - "dns.lan.${EXTERNAL_DOMAIN}"
    - "${TECHNITIUM_IP4}"
  keystores:
    pkcs12:
      create: true
      passwordSecretRef:
        name: technitium-cert-secrets
        key: PFX_PASSWORD
