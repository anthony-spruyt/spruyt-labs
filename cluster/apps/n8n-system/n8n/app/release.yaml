---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: n8n
spec:
  interval: 4h
  timeout: 15m
  chartRef:
    kind: OCIRepository
    name: n8n-ocirepo
    namespace: flux-system
  valuesFrom:
    - kind: ConfigMap
      name: n8n-values
  postRenderers:
    - kustomize:
        images:
          # renovate: datasource=docker depName=n8nio/runners
          - name: n8nio/runners
            newTag: "2.36.7"
            digest: sha256:7d4463a5bd2da20dcaab904a761196ffbc0699d46f5f812c100526e4956f1de1
        patches:
          - target:
              kind: Deployment
              name: n8n-worker
            patch: |-
              apiVersion: apps/v1
              kind: Deployment
              metadata:
                name: _
              spec:
                template:
                  spec:
                    volumes:
                      - name: runner-tmp
                        emptyDir: {}
                    containers:
                      - name: task-runner
                        # renovate: ignore
                        image: n8nio/runners
                        env:
                          - name: N8N_RUNNERS_TASK_BROKER_URI
                            value: http://localhost:5679
                          # Default 15s is shorter than the gap between tasks
                          - name: N8N_RUNNERS_AUTO_SHUTDOWN_TIMEOUT
                            value: "300"
                          - name: N8N_RUNNERS_AUTH_TOKEN
                            valueFrom:
                              secretKeyRef:
                                name: n8n-runner-secrets
                                key: N8N_RUNNERS_AUTH_TOKEN
                        volumeMounts:
                          - name: runner-tmp
                            mountPath: /tmp
                        securityContext:
                          capabilities:
                            drop:
                              - ALL
                          readOnlyRootFilesystem: true
                          runAsNonRoot: true
                          runAsUser: 1000
                        resources:
                          requests:
                            cpu: 10m
                            memory: 128Mi
                          limits:
                            cpu: 1000m
                            memory: 512Mi
          - target:
              kind: Deployment
              name: n8n-webhook
            patch: |-
              apiVersion: apps/v1
              kind: Deployment
              metadata:
                name: _
              spec:
                template:
                  spec:
                    containers:
                      - name: mcp-header-proxy
                        image: ghcr.io/anthony-spruyt/mcp-header-proxy:0.0.9@sha256:434e3f4c2b01936b353ded4692ac346118d6cc33857248778306465b6b9624cc
                        env:
                          - name: UPSTREAM_URL
                            value: http://localhost:5678
                          - name: LISTEN_ADDR
                            value: ":8080"
                        ports:
                          - containerPort: 8080
                            name: mcp-proxy
                            protocol: TCP
                        securityContext:
                          capabilities:
                            drop:
                              - ALL
                          readOnlyRootFilesystem: true
                          runAsNonRoot: true
                          runAsUser: 65534
                        resources:
                          requests:
                            cpu: 5m
                            memory: 16Mi
                          limits:
                            memory: 32Mi
                        livenessProbe:
                          httpGet:
                            path: /healthz
                            port: 8080
                          initialDelaySeconds: 5
                          periodSeconds: 30
                        readinessProbe:
                          httpGet:
                            path: /healthz
                            port: 8080
                          initialDelaySeconds: 2
                          periodSeconds: 10
          - target:
              kind: Service
              name: n8n-webhook
            patch: |-
              apiVersion: v1
              kind: Service
              metadata:
                name: _
              spec:
                ports:
                  - name: mcp-proxy
                    port: 8080
                    targetPort: 8080
                    protocol: TCP
