---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./sonarqube-secrets.sops.yaml
  - ./release.yaml
  # - ./network-policies.yaml
  # - ./vpa.yaml
configMapGenerator:
  - name: sonarqube-values
    namespace: sonarqube
    files:
      - values.yaml
configurations:
  - ./kustomizeconfig.yaml
