# Default values: https://github.com/coder/coder/blob/main/helm/coder/values.yaml
coder:
  annotations:
    reloader.stakater.com/auto: "true"
  env:
    - name: CODER_ACCESS_URL
      value: "https://code.${EXTERNAL_DOMAIN}"
    - name: CODER_PG_CONNECTION_URL
      valueFrom:
        secretKeyRef:
          name: coder-cnpg-cluster-app
          key: uri
    - name: CODER_OIDC_ISSUER_URL
      value: "https://auth.${EXTERNAL_DOMAIN}/application/o/coder/"
    - name: CODER_OIDC_CLIENT_ID
      valueFrom:
        secretKeyRef:
          name: coder-oauth-credentials
          key: CODER_OIDC_CLIENT_ID
    - name: CODER_OIDC_CLIENT_SECRET
      valueFrom:
        secretKeyRef:
          name: coder-oauth-credentials
          key: CODER_OIDC_CLIENT_SECRET
    - name: CODER_OIDC_SIGN_IN_TEXT
      value: "Sign in with Authentik"
    - name: CODER_OIDC_SCOPES
      value: "openid,profile,email"
    - name: CODER_OAUTH2_GITHUB_DEFAULT_PROVIDER_ENABLE
      value: "false"
    # AI Gateway defaults to enabled since v2.34.0 — keep it off, no providers configured
    - name: CODER_AI_GATEWAY_ENABLED
      value: "false"
    - name: CODER_PROMETHEUS_ENABLE
      value: "true"
    - name: CODER_PROMETHEUS_ADDRESS
      value: "0.0.0.0:2112"
    # Disable external STUN — use built-in DERP relay only
    # CODER_DERP_SERVER_RELAY_URL is set automatically by the chart using pod IP
    - name: CODER_DERP_SERVER_STUN_ADDRESSES
      value: "disable"
  image:
    repo: "ghcr.io/coder/coder"
    # Tag intentionally unset: the chart falls back to v{{ .Chart.AppVersion }},
    # which always equals the chart version pinned in release.yaml. Renovate
    # cannot see this key (helm-values does not recognise coder's repo/tag
    # shape), so a pinned tag silently drifts behind the chart on every bump.
    # Letting the chart drive it keeps server and chart in lockstep on the
    # stable channel, with one version to track. Ref #2640
    tag: ""
  resources:
    requests:
      cpu: 100m
      memory: 512Mi
    limits:
      memory: 2Gi
  service:
    type: ClusterIP
  serviceAccount:
    workspaceNamespaces:
      - name: coder-workspaces
        workspacePerms: true
        enableDeployments: true
