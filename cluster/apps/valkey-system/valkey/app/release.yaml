---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: valkey
spec:
  chart:
    spec:
      # renovate: datasource=github-releases depName=valkey-io/valkey-helm
      chart: valkey
      version: v0.8.1
      sourceRef:
        kind: HelmRepository
        name: valkey-io-charts
        namespace: flux-system
  valuesFrom:
    - kind: ConfigMap
      name: valkey-values
