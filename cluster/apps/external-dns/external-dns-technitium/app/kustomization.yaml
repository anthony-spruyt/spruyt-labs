---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./external-dns-technitium-secrets.sops.yaml
  - ./release.yaml
configMapGenerator:
  - name: external-dns-technitium-values
    namespace: external-dns
    files:
      - values.yaml
configurations:
  - ./kustomizeconfig.yaml
