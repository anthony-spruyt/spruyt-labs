---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./sungather-secrets.sops.yaml
  - ./release.yaml
configMapGenerator:
  - name: sungather-values
    namespace: sungather
    files:
      - values.yaml
configurations:
  - ./kustomizeconfig.yaml
