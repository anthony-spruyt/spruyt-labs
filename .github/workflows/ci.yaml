# yaml-language-server: $schema=https://raw.githubusercontent.com/SchemaStore/schemastore/master/src/schemas/json/github-workflow.json
# See https://github.com/anthony-spruyt/repo-operator/blob/main/src/templates/.github/workflows/ci.yaml for template comments

name: "CI"
on:
  pull_request:
    branches:
      - "main"
  push:
    branches:
      - "main"
concurrency:
  group: "${{ github.workflow }}-${{ github.event_name == 'push' && github.sha || github.ref }}"
  cancel-in-progress: "${{ github.event_name == 'pull_request' }}"
permissions:
  actions: "write"
  contents: "write"
  packages: "write"
  pull-requests: "write"
  security-events: "write"
  statuses: "write"
  id-token: "write"
  attestations: "write"
jobs:
  changes:
    name: "Detect changes"
    runs-on: "ubuntu-latest"
    outputs:
      cluster: "${{ steps.filter.outputs.cluster }}"
      kyverno: "${{ steps.filter.outputs.kyverno }}"
      terraform: "${{ steps.filter.outputs.terraform }}"
      shutdown-orchestrator: "${{ steps.filter.outputs.shutdown-orchestrator }}"
      kata-tap-qdisc-fix: "${{ steps.filter.outputs.kata-tap-qdisc-fix }}"
      mcp-header-proxy: "${{ steps.filter.outputs.mcp-header-proxy }}"
      llm-guard-adapter: "${{ steps.filter.outputs.llm-guard-adapter }}"
      agent-queue-worker: "${{ steps.filter.outputs.agent-queue-worker }}"
      bull-board: "${{ steps.filter.outputs.bull-board }}"
      claude-agents: "${{ steps.filter.outputs.claude-agents }}"
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@fe104658747b27e96e4f7e80cd0a94068e53901d
        with:
          egress-policy: audit
      - name: "Checkout"
        uses: "actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd" # v6
        with:
          fetch-depth: 0
      - name: "Find last successful CI commit"
        if: "github.event_name == 'push'"
        id: "last-success"
        env:
          GH_TOKEN: "${{ github.token }}"
          WORKFLOW: "CI"
        run: |
          SHA=$(gh run list --workflow "$WORKFLOW" --branch main --status success --limit 1 --json headSha --jq '.[0].headSha // empty')
          if [ -n "$SHA" ] && git cat-file -e "$SHA" 2>/dev/null; then
            echo "base=$SHA" >> "$GITHUB_OUTPUT"
          else
            echo "base=${{ github.event.before }}" >> "$GITHUB_OUTPUT"
          fi
      - name: "Path filter"
        uses: "dorny/paths-filter@fbd0ab8f3e69293af611ebaee6363fc25e6d187d" # v4
        id: "filter"
        with:
          base: "${{ steps.last-success.outputs.base }}"
          filters: |
            cluster:
              - 'cluster/**/*.yaml'
            kyverno:
              - 'cluster/apps/kyverno/**'
            terraform:
              - 'infra/terraform/**'
            shutdown-orchestrator:
              - 'cmd/shutdown-orchestrator/**'
            kata-tap-qdisc-fix:
              - 'cmd/kata-tap-qdisc-fix/**'
            mcp-header-proxy:
              - 'cmd/mcp-header-proxy/**'
            llm-guard-adapter:
              - 'cmd/llm-guard-adapter/**'
            agent-queue-worker:
              - 'ts/agent-queue-worker/**'
            bull-board:
              - 'ts/agent-queue-worker/bull-board/**'
            claude-agents:
              - 'cluster/apps/claude-agents-shared/**'
  lint:
    uses: "anthony-spruyt/repo-operator/.github/workflows/_lint.yaml@main"
    secrets: "inherit"
  kubeconform:
    needs: "changes"
    if: "needs.changes.outputs.cluster == 'true'"
    uses: "./.github/workflows/_kubeconform.yaml"
  kyverno-test:
    needs: "changes"
    if: "needs.changes.outputs.kyverno == 'true'"
    uses: "./.github/workflows/_kyverno-test.yaml"
  bats-test:
    needs: "changes"
    if: "needs.changes.outputs.claude-agents == 'true'"
    uses: "./.github/workflows/_bats-test.yaml"
  terraform-validate:
    needs: "changes"
    if: "needs.changes.outputs.terraform == 'true'"
    uses: "./.github/workflows/_terraform-validate.yaml"
  shutdown-orchestrator:
    needs:
      - "lint"
      - "changes"
    if: "needs.changes.outputs.shutdown-orchestrator == 'true'"
    uses: "./.github/workflows/_release-image.yaml"
    with:
      image: "shutdown-orchestrator"
      workdir: "cmd/shutdown-orchestrator"
      tag-prefix: "shutdown-orchestrator/v"
      language: "go"
      push: "${{ github.event_name == 'push' }}"
    secrets: "inherit"
  kata-tap-qdisc-fix:
    needs:
      - "lint"
      - "changes"
    if: "needs.changes.outputs.kata-tap-qdisc-fix == 'true'"
    uses: "./.github/workflows/_release-image.yaml"
    with:
      image: "kata-tap-qdisc-fix"
      workdir: "cmd/kata-tap-qdisc-fix"
      tag-prefix: "kata-tap-qdisc-fix/v"
      language: "go"
      push: "${{ github.event_name == 'push' }}"
    secrets: "inherit"
  mcp-header-proxy:
    needs:
      - "lint"
      - "changes"
    if: "needs.changes.outputs.mcp-header-proxy == 'true'"
    uses: "./.github/workflows/_release-image.yaml"
    with:
      image: "mcp-header-proxy"
      workdir: "cmd/mcp-header-proxy"
      tag-prefix: "mcp-header-proxy/v"
      language: "go"
      push: "${{ github.event_name == 'push' }}"
    secrets: "inherit"
  llm-guard-adapter:
    needs:
      - "lint"
      - "changes"
    if: "needs.changes.outputs.llm-guard-adapter == 'true'"
    uses: "./.github/workflows/_release-image.yaml"
    with:
      image: "llm-guard-adapter"
      workdir: "cmd/llm-guard-adapter"
      tag-prefix: "llm-guard-adapter/v"
      language: "go"
      push: "${{ github.event_name == 'push' }}"
    secrets: "inherit"
  agent-queue-worker:
    needs:
      - "lint"
      - "changes"
    if: "needs.changes.outputs.agent-queue-worker == 'true'"
    uses: "./.github/workflows/_release-image.yaml"
    with:
      image: "agent-queue-worker"
      workdir: "ts/agent-queue-worker"
      tag-prefix: "agent-queue-worker/v"
      language: "node"
      push: "${{ github.event_name == 'push' }}"
    secrets: "inherit"
  bull-board:
    needs:
      - "lint"
      - "changes"
    if: "needs.changes.outputs.bull-board == 'true'"
    uses: "./.github/workflows/_release-image.yaml"
    with:
      image: "bull-board"
      workdir: "ts/agent-queue-worker/bull-board"
      tag-prefix: "bull-board/v"
      language: "node"
      push: "${{ github.event_name == 'push' }}"
    secrets: "inherit"
  summary:
    needs:
      - "lint"
      - "kubeconform"
      - "kyverno-test"
      - "terraform-validate"
      - "shutdown-orchestrator"
      - "kata-tap-qdisc-fix"
      - "mcp-header-proxy"
      - "llm-guard-adapter"
      - "agent-queue-worker"
      - "bull-board"
      - "bats-test"
    if: "always()"
    uses: "anthony-spruyt/repo-operator/.github/workflows/_summary.yaml@main"
