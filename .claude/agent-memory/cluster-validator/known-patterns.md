# Known Patterns

## Operational Patterns

Timing, behavioral, and environmental knowledge learned from validation runs.

| Pattern | Context | Count | Last Seen | Added |
|---------|---------|-------|-----------|-------|
| firemerge dependency chain (firefly-iii → firemerge → traefik-ingress) takes 3-5 min to fully reconcile | Full cluster reconciliation wait | 11 | 2026-03-29 | 2026-02-24 |
| flux-operator upgrade triggers FluxInstance re-reconciliation (~3s) and OutdatedVersion event for flux | Normal behavior after operator upgrade | 6 | 2026-04-15 | 2026-02-25 |
| authentik dependency chain (authentik → many apps → traefik-ingress) settles within ~90s | Full cluster reconciliation wait after flux-system changes | 11 | 2026-04-15 | 2026-02-25 |
| CronJob validation requires manual test job -- last completed job ran previous version | CronJob workload type detection | 7 | 2026-04-04 | 2026-02-28 |
| YAML comment-only changes (e.g., schema directives, resource-sizing comments) reconcile instantly with no resource drift | Kustomize strips comments, producing identical output | 2 | 2026-03-17 | 2026-03-01 |
| Dashboard JSON reformatting (whitespace/indentation changes) reconciles as configmap update with no functional impact; Grafana/VictoriaMetrics reloads the dashboard without errors | Prettier/formatter cosmetic changes to JSON dashboard files | 1 | 2026-03-21 | 2026-03-21 |
| n8n queue-mode deployment (main + worker + webhook) all roll simultaneously on values change | HelmRelease values.yaml change (image tag, env vars, volumes) triggers rolling update of all 3 deployments | 7 | 2026-03-30 | 2026-03-03 |
| Large image first-pull (~1GB) can exceed 10m HelmRelease timeout; Flux auto-rollback then retries successfully with cached image | openclaw image update, image pulled in 10m1s exceeding timeout, auto-retry succeeded | 3 | 2026-03-16 | 2026-03-09 |
| Revert of failed kyverno policy change recovers full cluster within ~90s; Flux cleans up removed ClusterPolicy resources automatically | Revert commit after kyverno-policies reconciliation failure | 1 | 2026-03-09 | 2026-03-09 |
| Kyverno policy matching CRDs (e.g., HelmRelease) requires full GVK path `group/version/Kind` in `kinds` field; bare Kind name only works for core/built-in resources | Kyverno webhook registration, CRD matching | 1 | 2026-03-10 | 2026-03-10 |
| Kyverno `--crdWatcher=false` prevents webhook auto-registration of CRD-based resources; policy is accepted but webhook never routes CRD admission requests to Kyverno | Kyverno config, CRD policy, webhook rules | 3 | 2026-03-10 | 2026-03-10 |
| Kyverno chart v3.7.1 `config.crdWatcher` maps to ConfigMap, NOT container args; correct path is `admissionController.crdWatcher: true` | Helm values path mismatch, silent no-op | 1 | 2026-03-10 | 2026-03-10 |
| Helm silently ignores unknown/misplaced values keys -- always verify rendered manifest with `helm get manifest` | Helm chart values validation | 3 | 2026-03-26 | 2026-03-10 |
| Kyverno 1.17.x on K8s 1.35: webhooks not auto-updated for CRD-targeted policies due to v1alpha1.MutatingAdmissionPolicy reflector failures (upstream kyverno/kyverno#15362) | Kyverno webhook, CRD policy, K8s API version mismatch | 1 | 2026-03-10 | 2026-03-10 |
| Kyverno downgrade (3.7.1->3.6.1 / v1.17->v1.16) succeeds cleanly; crdWatcher works in v1.16.1 on K8s 1.35; webhook auto-registers CRD resources within seconds of policy creation | Kyverno version workaround, crdWatcher validation | 1 | 2026-03-10 | 2026-03-10 |
| Kyverno minor chart upgrade (3.6.1->3.6.3 / v1.16.1->v1.16.3) reconciles within ~3 min; migration job runs and completes; downstream deps (kyverno-policies, descheduler, qdrant) settle immediately after | Kyverno patch upgrade, helm-release type | 1 | 2026-03-10 | 2026-03-10 |
| Kyverno chart v3.6.3 places container resources under `admissionController.container.resources` (not `admissionController.resources`); same pattern for background/cleanup/reports controllers | Kyverno Helm values path, resource limits | 1 | 2026-03-26 | 2026-03-26 |
| Kyverno mutate policy with `+(anchor)` only fires on CREATE/UPDATE; existing resources need forced reconciliation to receive mutations | Kyverno admission-only mutation, no background | 2 | 2026-04-02 | 2026-03-10 |
| Talos extraManifests CRD updates via SSA: bundle-version annotations on existing CRDs may not update due to field ownership conflict between `talos` manager and previous manager; CRD schemas are updated correctly | Talos config, CRD update, SSA field ownership | 1 | 2026-03-10 | 2026-03-10 |
| Middleware base template with Flux envsubst deploys to all consuming namespaces simultaneously; verify substitution in multiple namespaces | lan-ip-whitelist shared middleware, kustomize base pattern | 1 | 2026-03-12 | 2026-03-12 |
| SSE endpoints return HTTP 200 but curl hangs (timeout) because connection is long-lived; use short --max-time and check headers for `text/event-stream` | MCP VM SSE endpoint validation | 1 | 2026-03-12 | 2026-03-12 |
| OCIRepository secretRef addition reconciles instantly with no downstream impact; source controller re-authenticates on next poll cycle without triggering pod rollouts | Flux OCI source auth config change, ghcr-docker-config secret | 1 | 2026-03-16 | 2026-03-16 |
| cowboysysop VPA chart installs CRDs via a pre-install Job (`vertical-pod-autoscaler-crds`) using `bitnamilegacy/kubectl` image; CRD creation takes ~13s; all 3 VPA pods start after CRDs are ready | VPA initial deployment, helm-release type | 1 | 2026-03-17 | 2026-03-17 |
| Large-scale VPA rollout (50 files, 86 objects) reconciles within ~5 min across all namespaces; VPA objects with `updateMode: Off` show recommendations within minutes of creation; newly created VPAs may show empty status initially | VPA workload rollout, kustomization type | 2 | 2026-03-19 | 2026-03-18 |
| cert-manager version upgrade rolls all 3 deployments (controller, cainjector, webhook) in ~20-30s; webhook regenerates TLS certs; ACME registrations re-verified; all certificates remain Ready | cert-manager helm-release upgrade (v1.19.4->v1.20.0, v1.20.0->v1.20.1) | 2 | 2026-03-29 | 2026-03-18 |
| cert-manager upgrade can leave wildcard certificate in InProgress state; traefik KS health check times out waiting for Certificate/traefik/wildcard-spruyt-xyz; traefik-ingress blocked as downstream dependency; ACME order enters `invalid` state and does not self-heal | cert-manager upgrade (PR #807 aac39cf5), certificate renewal stall; persists across subsequent reconciliation waves; ZeroSSL ACME order invalid since 2026-02-23, cert expired 2026-03-25 | 3 | 2026-03-29 | 2026-03-29 |
| Spegel DaemonSet rolling update across 6 nodes completes in ~60s; transient startup probe 500s are normal (~5s per pod); P2P bootstrap connectivity takes ~5s per pod | Spegel helm-release config change, DaemonSet rollout | 1 | 2026-03-19 | 2026-03-19 |
| Spegel chart (spegel@0.6.0) silently accepts `verticalPodAutoscaler` values but does NOT template VPA resources; a standalone VPA manifest is needed for VPA monitoring of Spegel | Helm chart values no-op, VPA integration | 1 | 2026-03-17 | 2026-03-17 |
| Preflight idle mode (MODE=preflight) keeps pod alive on :8080 serving /healthz after checks pass; v1.1.0 fixed the exit-after-check bug that caused CrashLoopBackOff in v1.0.0 | shutdown-orchestrator preflight mode, health probes | 4 | 2026-03-25 | 2026-03-23 |
| Rook-ceph cross-namespace RBAC requires separate Flux kustomization with its own targetNamespace; without it, Role/RoleBinding deploy to the app's namespace instead of rook-ceph | shutdown-orchestrator RBAC split, kustomization targetNamespace | 1 | 2026-03-23 | 2026-03-23 |
| Cross-namespace SA + token Secret deployed via same kustomization as target namespace; RoleBinding in target NS references SA in source NS via subjects[].namespace field | claude-agents RoleBinding referencing n8n-claude-spawner SA in n8n-system; SA + token Secret created in n8n-system via n8n kustomization | 1 | 2026-03-30 | 2026-03-30 |
| Build-time-only changes (go.mod, go.sum, Dockerfile) reconcile instantly with no runtime impact; running pod retains previous image until container image is rebuilt and tag updated | shutdown-orchestrator go.mod Go version bump | 1 | 2026-03-25 | 2026-03-25 |
| ConfigMap hash change (configMapGenerator) triggers pod rollout even when HelmRelease version unchanged; verify new pod logs after values.yaml env var changes | mcp-victoriametrics SSE-to-HTTP mode switch, configMapGenerator hash; openclaw gateway auth/config changes; openclaw init-skills script changes; openclaw env var additions; openclaw JSON config changes; openclaw subagent spawn permissions; openclaw exec approval + channel config; openclaw heartbeat disable; openclaw agent workspace fields; openclaw shfmt formatting; headlamp plugin version bump via pluginsManager configContent; openclaw NODE_OPTIONS heap size + memory limit increase; n8n spawner volume mounts | 20 | 2026-03-30 | 2026-03-13 |
| flux-operator overrides flux-system namespace labels; labels declared in cluster/apps/flux-system/namespace.yaml are not applied because flux-operator manages the namespace with `app.kubernetes.io/managed-by: flux-operator`; use kustomize patch in flux-instance values.yaml instead | flux-system namespace, descheduler label exclusion | 2 | 2026-03-13 | 2026-03-13 |
| Cilium CNI patch upgrade (v1.19.x) rolling restart completes within ~4 min; BGP re-establishes immediately; transient Hubble event queue full warning is normal during agent restart | Cilium helm-release patch upgrade, DaemonSet rollout | 1 | 2026-03-25 | 2026-03-25 |
| Application image upgrade with breaking Python import error (AttributeError on module restructure) causes CrashLoopBackOff; app self-reports old version string — verify image changelog before deploying major version bumps | sungather v0.5.3 to v1.0.2, SungrowClient module restructure; v1.0.4 fixed it | 2 | 2026-03-28 | 2026-03-28 |
| Helm upgrade during CrashLoopBackOff takes near-full timeout (~10m-15m) to roll over; Flux auto-rollback then retries; may take 2+ failed upgrades before fix lands if multiple commits are pushed | sungather rollback from 1.0.2 to v0.5.3 while CrashLoopBackOff; openclaw OOM fix required 3 commits (254d89a7, 03c74721, cdc0406b) with 2 failed upgrades + auto-rollbacks before v49 succeeded | 2 | 2026-03-30 | 2026-03-28 |
| CNPG operator chart upgrade triggers rolling restart of all managed Postgres instances to update bootstrap container image; clusters transition through "Upgrading cluster" phase and settle to healthy within ~2 min; transient "database system is starting up" messages are expected | CNPG chart v0.27.1->v0.28.0, operator 1.28.1->1.29.0, 3 clusters (authentik, firefly-iii, n8n) | 1 | 2026-04-03 | 2026-04-03 |
| Large digest-pin PRs (23+ files, 16+ namespaces) reconcile within minutes with no pod disruption when images are already cached; Flux applies updated manifests but pods only restart if digest actually changed | Renovate bulk pin-dependencies PR #809, 26 container images across 16 namespaces; CNPG image digest pin #813, 3 clusters | 2 | 2026-03-29 | 2026-03-29 |
| sungather v1.0.4 with HTTP /health probes starts cleanly in ~36s; no AttributeError, SungrowClient import fixed | sungather image upgrade fix verification | 1 | 2026-03-28 | 2026-03-28 |
| New app deployment with CNPG dependency: initial Helm install times out (~10m) due to CNPG init + app startup race; Flux auto-retries upgrade which succeeds; kustomization health check fails on stale InProgress status, needs manual `flux reconcile` to clear | Coder first deployment PR #873; CNPG cluster init (PVC provisioning, initdb, multi-attach detach cycle) takes ~30s; app CrashLoopBackOff during DB init; upgrade succeeds once DB healthy | 1 | 2026-04-04 | 2026-04-04 |
| Rook-ceph patch upgrade (v1.19.x) via OCIRepository bump: cluster chart upgrades first and settles fast; operator HR takes ~60s longer (single operator pod rollout); no OSD/mon restart, HEALTH_OK maintained throughout; Ceph daemon versions unchanged (patch = operator/sidecar only) | Renovate rook-ceph group v1.19.3->v1.19.4 PR #917 | 1 | 2026-04-15 | 2026-04-15 |
| configMapGenerator `key=source` syntax: the LHS becomes the ConfigMap data key and must match regex `[-._a-zA-Z0-9]+` — paths containing `/` (e.g. `templates/devcontainer/main.tf`) cause dry-run Invalid errors. Fix: use flat keys like `devcontainer__main.tf=./templates/devcontainer/main.tf` and rehydrate subdirs at runtime via `volumes[].configMap.items[].path` mapping each flat key to its original relative path | coder-template-sync issue #934; confirmed Ready at 5dbfe233 and Job coder-template-push Completed 1 template | 2 | 2026-04-15 | 2026-04-15 |
| CNPG plugin-barman-cloud chart minor bump (0.5.0->0.6.0) reconciles via single Helm upgrade (~60-120s); single deployment rollout, no CNPG cluster disruption; metadata-only chart changes reconcile cleanly | Renovate plugin-barman-cloud 0.5.0->0.6.0 PR #920 | 1 | 2026-04-15 | 2026-04-15 |
| victoria-metrics-operator chart minor (0.59.3->0.60.0) with pre-merge orphan-delete workaround: new Deployment created with new selector labels, rolling update proceeds normally, old RSes scale to 0/0 with no cleanup needed; operator reconciles all managed workloads (vmagent/vmalert/vmalertmanager) to new config-reloader image within ~30s of pod ready | Renovate PR #919, issue #922, selector-rename pattern (same as #916 victoria-logs-single) | 1 | 2026-04-15 | 2026-04-15 |
| Crafty-controller image patch bump (arcadiatechnology/crafty-4 4.10.2->4.10.3) via values.yaml: StatefulSet pod rolls ~2-3 min; Helm upgrade completes to v14; no PVC disruption | Renovate PR #913, issue #922, crafty-controller StatefulSet | 1 | 2026-04-15 | 2026-04-15 |
| victoria-metrics-k8s-stack chart minor bump (0.72.6->0.73.0) with bundled vmsingle/vmagent/vmalert v1.140.0 app images: Helm upgrade takes ~60-120s; operator-managed workloads roll smoothly; no selector-rename issues on this chart bump | Renovate PR #911, issue #922, commit f324ba5b | 1 | 2026-04-15 | 2026-04-15 |
| falco helm chart patch bump (8.0.1->8.0.2) triggers DaemonSet rolling restart across all nodes; Helm upgrade takes ~2-3 min during the 'Running upgrade action' phase; falcosidekick deployment rolls in parallel; no eBPF disruption | Renovate PR #905, issue #922, commit 2a565e2f | 1 | 2026-04-15 | 2026-04-15 |
| coder helm chart patch bump (2.30.6->2.30.7) rolls single coder Deployment pod within ~60-120s; Helm release advances to v3; CNPG cluster and workspace/provisioner pods unaffected; no DB migration | Renovate PR #903, issue #922, commit 5a1ad861 | 1 | 2026-04-15 | 2026-04-15 |
| flux-operator + flux-instance minor bump (0.45.1->0.46.0) reconciles via rolling flux-operator pod (~2 min), FluxInstance re-reconciles in ~8s, no flux-system controller disruption; downstream KS dependency chain settles within one 60s wait cycle | Renovate PRs #892 + #891, issue #922, commits 2137a40c + c4737865 | 1 | 2026-04-15 | 2026-04-15 |
| Paired small Renovate image-tag PRs (mcp-discord patch + n8n minor) reconcile in parallel within a single 60s wait cycle; n8n HR briefly shows `Running 'upgrade' action` then settles; discord-mcp Deployment rolls single pod; HR revisions unchanged (values-only image tag bump) | Renovate PRs #887 + #888, issue #922, commits eb09c76d + a4883820 | 1 | 2026-04-15 | 2026-04-15 |
| vaultwarden image tag switch (testing-alpine -> 1.35.7-alpine stable) via values.yaml triggers single STS pod roll within ~60-120s; HelmRelease advances one revision (v23->v24); pod /alive probe passes immediately; data PVC retained | Issue #929, commit d6507c00, vaultwarden StatefulSet | 1 | 2026-04-15 | 2026-04-15 |
| vmsingle resource bump via values.yaml (limits/requests + extraArgs flag) triggers single pod roll within ~60s; operator reconciles Deployment; new pod args include extraArgs entry (e.g., `-search.maxMemoryPerQuery=1GB`); HelmRelease advances one revision; brief connection-refused on old pod during shutdown is expected scrape gap | victoria-metrics-k8s-stack vmsingle memory bump issue #958, commit 4ef29dd7 (2Gi->4Gi limit, 1664Mi->2Gi request, +search.maxMemoryPerQuery=1GB) | 1 | 2026-04-16 | 2026-04-16 |
| Falco additional rules ConfigMap pattern: new ConfigMap in falco-system + HelmRelease values additions to `mounts.volumes` (ConfigMap ref) and `mounts.volumeMounts` (mountPath `/etc/falco/rules.d/<rule>.yaml`, subPath) triggers full DaemonSet rolling restart across all nodes within ~3-4 min; all pods load the new rule file with `schema validation: ok`; pre-existing rule mounts (e.g. `exceptions.yaml`) preserved | falco audit rule for Kata setns issue #961, commit 1c4b23da — 6 DS pods recreated, 0 restarts, 2/2 Ready, `kata-tap-qdisc-fix.yaml` loaded alongside existing `exceptions.yaml` | 1 | 2026-04-17 | 2026-04-17 |
| Namespace-split refactor (13 commits) moving workloads + SOPS secrets + CNPs + RBAC across namespaces reconciles in one Flux wave: new ns created with PSA=privileged + descheduler-exclude labels, 14 CNPs materialize Valid=True, 5 SOPS secrets decrypt, new Kyverno ClusterPolicy Admission=true/Ready=True, all 7 coder kustomizations Ready @ HEAD within ~60s; old-namespace pods retained (no accidental prune) because workload pods not moved in this wave | Coder workspaces namespace split (Option B) issue #977, commits 7875a1b3..c1dfde27 — coder-workspaces ns created with 14 CNPs, 5 secrets, SA, Kyverno ClusterPolicy `restrict-privileged-to-coder-workspace-sa` (SA-scoped privileged admission), Task 14 (workspace pod restarts) intentionally deferred to user via Coder UI | 1 | 2026-04-17 | 2026-04-17 |

## Failure Signatures

Error patterns and their known resolutions.

| Error Pattern | Root Cause | Resolution | Count | Last Seen | Added |
| falcosidekick liveness probe `context deadline exceeded` on `/ping` endpoint causing CrashLoopBackOff | CPU limit too low (75m) causes throttling; probe times out waiting for HTTP response | Increase CPU limit (75m -> 250m); update VPA maxAllowed to match | 1 | 2026-04-04 | 2026-04-04 |
|---------------|------------|------------|-------|-----------|-------|
| `exec: "/bin/sh": stat /bin/sh: no such file or directory` in CronJob pod | Scratch-based container image (e.g., `ghcr.io/siderolabs/talosctl`) has no shell | Use an image with a shell or restructure command to avoid shell | 1 | 2026-02-28 | 2026-02-28 |
| `/bin/sh: openssl: not found` in CronJob pod using `alpine/k8s` image | `alpine/k8s` ships kubectl/helm/kustomize but NOT openssl; JWT signing (RS256) requires openssl | Switch to image with openssl or install at runtime; verify all script dependencies exist in chosen image | 1 | 2026-03-31 | 2026-03-31 |
| `/bin/sh: ssh-keygen: not found` in CronJob pod using `alpine/k8s` image | `alpine/k8s` ships kubectl/helm/kustomize but NOT openssh-keygen; SSH key generation requires openssh | Switch to image with openssh-keygen or add `apk add --no-cache openssh-keygen` init step; verify all script dependencies exist in chosen image | 1 | 2026-04-04 | 2026-04-04 |
| Flux envsubst mangles awk `$N` field references in inline scripts | Flux post-build `envsubst` treats `$N` as variable references even inside YAML strings/single-quoted awk | Escape all awk field refs as `$$N` in Flux-managed YAML (e.g., `$$2`, `$$8`) | 2 | 2026-02-28 | 2026-02-28 |
| Flux postBuild envsubst replaces `${VAR}` in ALL string fields including JSON config values; use `$${VAR}` to produce literal `${VAR}` for runtime env-var resolution by apps | openclaw.json env-var refs for hooks.token and gateway.token were being substituted to empty strings by Flux | 1 | 2026-03-15 | 2026-03-15 |
| `spec.interval: Required value` on HelmRelease dry-run after moving defaults from Flux patches to Kyverno mutating policy | Kyverno admission webhooks fire AFTER server-side apply dry-run validation; CRD-required fields must be present in the manifest before dry-run | Keep required fields (spec.interval) in Flux kustomization patches or set them explicitly in each HelmRelease; Kyverno +(anchor) mutation cannot inject CRD-required fields | 3 | 2026-03-12 | 2026-03-09 |
| OOMKilled (exit code 137) on mcp-victoriametrics with 128Mi-1Gi limits | Container memory limit too low for application startup; process killed within 7s after single log line | Increase memory limit aggressively (2Gi worked); mcp-victoriametrics v1.18.0 in SSE mode needs >1Gi at startup | 4 | 2026-03-12 | 2026-03-12 |
| HelmRelease stuck in `pending-upgrade` with no successful revision history | All revisions failed (install + upgrades), Flux error: `missing target release for rollback: cannot remediate failed release` | Manual `helm rollback <release> <revision> -n <ns>` to unstick, then Flux retries with new values | 1 | 2026-03-12 | 2026-03-12 |
| `Startup probe failed: HTTP probe failed with statuscode: 404` on FastMCP-based apps | FastMCP streamable-http transport serves only `/mcp` endpoint; no `/health` path exists | Switch probes from `httpGet` to `tcpSocket` on the app port | 1 | 2026-03-15 | 2026-03-15 |
| Traefik chart v39.0.5 `experimental.localPlugins` requires `type` field (`hostPath`, `inlinePlugin`, or `localPath`); bare `moduleName` alone causes template error | Chart template `_helpers.tpl` `getLocalPluginType` enforces type selection | Add `type: localPath` with `volumeName` and `mountPath`; chart auto-creates volumeMount | 1 | 2026-03-15 | 2026-03-15 |
| Renovate container image bump to non-existent tag causes ImagePullBackOff; old pod already terminated by rolling update leaving service DOWN | Renovate auto-merge before image published to GHCR | Revert commit; retry with correct tag once available | 3 | 2026-03-16 | 2026-03-16 |
| SOPS secret with unquoted integer values in `stringData` fails Flux dry-run: `expected string, got &value.valueUnstructured{Value:NNNN}` | SOPS preserves YAML types; unquoted numbers remain integers after decryption; Kubernetes `stringData` requires string type | Quote numeric values as strings in SOPS file: `field: "12345"` instead of `field: 12345` | 1 | 2026-03-31 | 2026-03-31 |
| SOPS secret using `data:` with plain string values fails Flux dry-run: `illegal base64 data at input byte NN` | SOPS `type:str` values are plain text after decryption; K8s `.data` requires base64-encoded values; `.stringData` accepts plain strings | Change `data:` to `stringData:` in the SOPS file via `sops <file>` editor | 1 | 2026-04-01 | 2026-04-01 |
| Helm upgrade fails with `cannot patch <sts> with kind StatefulSet: Forbidden: updates to statefulset spec for fields other than ... are forbidden` | Chart version changes `spec.selector.matchLabels` (immutable field); common when upstream chart renames custom labels (e.g., `app` → `app.kubernetes.io/component`) | Revert chart bump; for re-attempt, `kubectl delete statefulset <name> -n <ns> --cascade=orphan` + `kubectl delete pod <pod>` before pushing bump; next Flux reconciliation creates new STS with new selector and existing PVC is reattached — verified working on victoria-logs-single 0.11.31->0.11.32 (issue #922, commit aa88e8b6) | 3 | 2026-04-15 | 2026-04-15 |
| victoria-logs-single chart 0.11.32 StatefulSet selector immutability failure: rollback via git revert succeeds; Flux Helm rollback to previous release (v46->v48) restores pod health within ~60s; orphan-delete workaround + re-bump succeeds on retry — new STS adopts existing PVC server-volume-victoria-logs-single-server-0 (data preserved) | Renovate PR #916 victoria-logs-single 0.11.32 chart bump, revert commit 53ec8cc1; retry via issue #922 commit aa88e8b6 | 2 | 2026-04-15 | 2026-04-15 |
| Revert commit can be superseded by a newer commit pushed before Flux finishes reconciling the revert; Flux picks up the latest revision, skipping the intermediate revert | Fast-follow commit after revert, Flux git source polling | Verify the final deployed revision matches the actual HEAD, not the revert commit | 1 | 2026-03-16 | 2026-03-16 |
| GitHub Actions reusable workflow `startup_failure`: top-level `env:` block in `workflow_call` files causes immediate startup failure with no jobs created | Reusable workflow YAML syntax, repo-operator migration | Move `env:` block into `jobs.<id>.env` or individual steps; top-level `env:` is only valid in non-reusable workflows | 1 | 2026-03-21 | 2026-03-21 |
| GitHub Actions reusable workflow 403 on `gh api` for actions/runs: calling workflow has `actions: write` but reusable workflow's own `permissions` block overrides it, giving only `contents: read` | repo-operator `_summary.yaml`, permissions inheritance | Add `actions: read` to the reusable workflow's `permissions` block | 1 | 2026-03-21 | 2026-03-21 |
| Missing `targetNamespace` in ks.yaml causes "namespace not specified" error for namespace-scoped resources (e.g., VPA); Flux applies the resource without a namespace and the API server rejects the patch | csi-addons-controller-manager ks.yaml missing targetNamespace | Add `targetNamespace: <namespace>` to ks.yaml spec; fix reconciles within seconds | 2 | 2026-03-18 | 2026-03-18 |
| DaemonSet pod that uses `setns()` into host network namespaces under `/run/netns/cni-*` fails every iteration with `operation not permitted` when pod lacks `hostNetwork: true` / `hostPID: true` and runs as non-root (UID 65534) — NET_ADMIN + SYS_ADMIN capabilities alone are insufficient; HTTP liveness/readiness probes still pass (Ready=True) so symptoms are invisible from pod state alone, verify via logs | kata-tap-qdisc-fix v0.1.0 DaemonSet issue #951, commit b4693597 — worker threads poisoned on every sweep (108 netns × EPERM per 30s), pod Running but functionally inert | Add `hostNetwork: true` + `hostPID: true` to podSpec (defaultPodOptions in app-template values); may also need to relax `runAsNonRoot`/UID or allow extra syscalls via seccomp to grant `setns(CLONE_NEWNET)` permission | 1 | 2026-04-16 | 2026-04-16 |
| Shell script packaged via `configMapGenerator.files` and consumed as a Job container command gets ALL `${VAR}` references collapsed to empty strings by Flux postBuild envsubst, including intra-script shell locals defined earlier in the file; escape comment `# written as $${VAR}` in source does NOT survive because envsubst runs once and strips the $-escape in the same pass. Visible symptom: `curl: (22) error 404` on URL templates with empty path segments (e.g., `jq-/jq-linux-amd64`) and/or `[ "" = "200" ]` conditionals failing silently | nexus-provision-repos Job issue #968, commits 8be79e8e + 607edee8 — script used `$${JQ_VERSION}`, `$${NEXUS_URL}`, `$${AUTH}` etc but rendered ConfigMap shows empty everywhere; jq download URL became `jq-/jq-linux-amd64` → 404 | Options: (1) bake script into a custom image and reference by digest, (2) drop cluster-settings substituteFrom on this specific Kustomization, (3) use `postBuild.substitute` with an explicit non-substitute list, or (4) move `${}` expansions to genuine environment variables injected into the Job container (where Flux never touches them) | 1 | 2026-04-17 | 2026-04-17 |

## False Positives

Things that look like failures but aren't — avoid flagging these.

| Signal | Why It's Not a Problem | Count | Last Seen | Added |
|--------|----------------------|-------|-----------|-------|
| Kustomization firemerge not ready during reconciliation wave | Dependency chain, resolves within 5 min — wait for full cluster reconciliation | 10 | 2026-04-02 | 2026-02-24 |
| traefik-ingress shows DependencyNotReady briefly during reconciliation wave | Normal dependency ordering, resolves within seconds; persistent when traefik KS stuck on cert health check; persistent when new upstream dependency (coder) is not ready | 34 | 2026-04-04 | 2026-02-25 |
| Multiple kustomizations show "dependency authentik is not ready" during reconciliation | authentik dependency chain, resolves within ~90s — not a failure | 19 | 2026-04-15 | 2026-02-25 |
| mcp-victoriametrics shows "dependency victoria-metrics-k8s-stack is not ready" during reconciliation wave | Dependency chain, resolves within 2 min — parent KS already Ready but child hasn't re-evaluated yet | 2 | 2026-04-02 | 2026-03-19 |
| authentik 2026.2.0 logs `AttributeError("'Version' object has no attribute '__dict__'")` on startup | Upstream bug, warning-level only, does not affect functionality — API returns 200 | 1 | 2026-02-25 | 2026-02-25 |
| authentik default OAuth Mapping uses deprecated `ak_groups` — emits deprecation warning on outpost proxy requests | Not a failure — requests succeed with HTTP 200. Migrate to `User.groups` in admin UI | 1 | 2026-02-25 | 2026-02-25 |
| vmagent scrape failures for Grafana during k8s-stack upgrade | Pod IP changes during rollover cause transient scrape timeouts/connection refused — resolves once new pod is ready | 1 | 2026-02-25 | 2026-02-25 |
| openclaw startup probe fails with "connection refused" for ~20s after pod start | App takes ~20s to bind port 18789 — startup probe catches this normally, pod becomes ready within 30s | 27 | 2026-03-30 | 2026-02-25 |
| openclaw gateway token auth: `token_missing` and `pairing required` WS rejections after switching to token mode | Expected behavior — clients must complete token pairing via Control UI; not a failure | 1 | 2026-03-14 | 2026-03-14 |
| openclaw `Proxy headers detected from untrusted address` when trustedProxies doesn't include Traefik pod CIDR | Gateway ignores X-Forwarded-For from non-trusted IPs; functional but loses real client IP detection | 1 | 2026-03-14 | 2026-03-14 |
| n8n readiness probe "connection refused" for ~20s after pod start during rolling update | n8n takes ~20s to bind port 5678 after container start — transient, resolves once app initializes | 7 | 2026-03-30 | 2026-03-03 |
| n8n `DB_POSTGRESDB_SSL_CA_FILE` whitespace warning on startup | Pre-existing config issue, does not affect DB connectivity | 6 | 2026-03-30 | 2026-03-03 |
| n8n `N8N_RUNNERS_ENABLED` deprecation warning | Env var no longer needed in newer versions — cosmetic, not functional | 6 | 2026-03-30 | 2026-03-03 |
| n8n worker runs DB migrations on image upgrade before starting task broker; migrations complete within seconds and don't block other pods | n8n v2.13.4 ran 2 migrations (ChangeWorkflowPublishedVersionFKsToRestrict, AddTypeToChatHubSessions) | 1 | 2026-03-29 | 2026-03-29 |
| cloudflared connIndex=3 `control stream encountered a failure while serving` during startup | Transient QUIC reconnect on 4th tunnel connection — self-resolves within seconds, other 3 connections healthy | 1 | 2026-03-13 | 2026-03-13 |
| openclaw `/ready` returns HTTP 503 for ~5 min after startup due to health-monitor grace periods (startup-grace: 60s, channel-connect-grace: 120s) | App health-monitor waits for Discord channel connection before reporting ready; switching readiness probe to `/healthz` avoids this issue entirely | 3 | 2026-03-14 | 2026-03-13 |
| headlamp session-ttl crash fixed in v0.41.0; `sessionTTL: null` workaround removed; chart default `-session-ttl=86400` now works correctly (upstream kubernetes-sigs/headlamp#4883 resolved) | HelmRelease values cleanup, helm-release type | 2 | 2026-03-26 | 2026-03-14 |
| FastMCP 3.0.2 streamable-http transport has no `/health` endpoint; only `/mcp` responds (406 without MCP headers); use `tcpSocket` probes instead of `httpGet` | kubectl-mcp-server probe config, FastMCP health check | 1 | 2026-03-15 | 2026-03-15 |
| Traefik chart 39.x installs Gateway API CRDs by default; on clusters with `safe-upgrades.gateway.networking.k8s.io` VAP, set `install.crds: Skip` and `upgrade.crds: Skip` in HelmRelease | Traefik HR CRD management, Gateway API VAP conflict | 1 | 2026-03-15 | 2026-03-15 |
| MCP detection tools (certs_detect_tool, cilium_detect_tool etc.) previously failed with Forbidden due to missing apiextensions.k8s.io CRD read access; resolved by adding CRD read rule to ClusterRole (commit c157a8cc, issue #696) | kubectl-mcp-server RBAC, MCP tool detection | 2 | 2026-03-18 | 2026-03-18 |
| `check_crd_exists` MCP tool may return false negatives for CRDs that exist; verify with `kubectl get crd <name>` as fallback | VPA CRD existed but check_crd_exists returned false | 1 | 2026-03-18 | 2026-03-18 |
| OpenClaw hooks feature requires OPENCLAW_HOOKS_TOKEN in openclaw-secrets.sops.yaml; config uses env-var substitution and gateway refuses to start if hooks.enabled=true with empty token | openclaw hooks config, missing secret key | 2 | 2026-03-15 | 2026-03-15 |
| SOPS secret change for alertmanager config triggers config reload without pod restart; verify via "Loading configuration file" / "Completed loading" log lines | VMAlertmanager config reload, SOPS secret update | 1 | 2026-03-15 | 2026-03-15 |
| Alertmanager webhook DNS failure (`no such host`) produces cascading self-referential alerts (AlertmanagerFailedToSendAlerts, AlertmanagerClusterFailedToSendAlerts) that resolve automatically once the config is corrected | Alertmanager webhook misconfiguration, openclaw service name | 1 | 2026-03-15 | 2026-03-15 |
| cert-manager controller logs "owning resource not found in cache" for completed Orders/CertificateRequests on startup | Benign cache population during controller restart; references historical completed resources | 1 | 2026-03-18 | 2026-03-18 |
| Cilium L7 DNS rule `matchPattern: "github.com"` causes first curl to FQDN to hang ~5-8 min while Cilium populates FQDN cache; subsequent calls are fast | github-token-rotation CronJob first run, toFQDNs cold start with L7 DNS proxy | 1 | 2026-03-31 | 2026-03-31 |
| ESO ExternalSecret transient `UpdateFailed: SecretStore not ready` resolves within 1s when SecretStore and ExternalSecret are created simultaneously | ESO race condition during initial deployment, not a failure | 2 | 2026-04-04 | 2026-03-31 |
| Reloader auto-restarts pods when referenced secrets change; token rotation CronJob triggers cascading pod restarts via Reloader annotation `reloader.stakater.com/auto: true` | openclaw pod restarted after github-bot-credentials secret updated by rotation job | 2 | 2026-03-31 | 2026-03-31 |
| Large namespace split (76 files, 4 new namespaces, namespace removal) reconciles within ~2 min; Flux prunes old namespace resources and creates new ones atomically | github-app-token-rotation feature, claude-agents namespace split | 1 | 2026-03-31 | 2026-03-31 |
| CronJob scheduled runs that fire between pushes may use old code and fail; verify `lastSuccessfulTime` vs `lastScheduleTime` and check if failures predate the current revision | github-token-rotation 22:17 and 22:30 runs failed with pre-fix code; manual test with post-fix code succeeded; next scheduled run (23:00) will use fixed code | 1 | 2026-03-31 | 2026-03-31 |
| Shared kustomize base with SOPS secrets deploys to multiple namespaces simultaneously; SOPS decryption and resource creation verified within seconds of reconciliation | claude-agents-shared base deployed to claude-agents-read and claude-agents-write; mcp-credentials SOPS secret, CiliumNetworkPolicies, RBAC, ExternalSecrets all created in both namespaces | 2 | 2026-04-03 | 2026-04-02 |
| Files in `cluster/apps/<ns>/<app>/assets/` are NOT auto-rendered into ConfigMaps; only files referenced by the app's kustomization (`configMapGenerator`, `resources`) are deployed. Edits to assets consumed at app runtime (e.g., n8n workflow prompt files) produce no cluster-side changes and no pod rollout | n8n sre-triage-prompt.md edit (issue #926, commit 2108bbaa) — expected ConfigMap hash bump never occurred because file isn't referenced by app kustomization | 1 | 2026-04-15 | 2026-04-15 |
| Packaging Terraform/HCL source into a Flux-managed ConfigMap (via `configMapGenerator.files`) causes postBuild envsubst to fail with `missing closing brace` because HCL `${...}` interpolations contain nested parens/commas/quotes that envsubst cannot parse. Whole Kustomization build fails and zero resources apply. Fix: escape every `${` to `$${` in the source file so envsubst emits a literal `$` and Terraform sees original syntax; alternatively bake templates into a container image instead of a ConfigMap | coder-template-sync issue #934, commit 95cd8348 — main.tf with `${replace(element(split(...)))}` expressions broke the entire kustomization; parent cluster-apps applies substituteFrom to all children unconditionally | 1 | 2026-04-15 | 2026-04-15 |
| Kustomize `configMapGenerator.files` entries of form `key=path` propagate the literal left-hand string as the ConfigMap data key. If the key contains `/` (e.g., `templates/devcontainer/main.tf=./path`), kustomize builds but the server-side ConfigMap dry-run rejects it with `a valid config key must consist of alphanumeric characters, '-', '_' or '.'`. Fix: flatten keys (e.g., `main.tf=./templates/devcontainer/main.tf`) or use a subchart/container image if directory structure must be preserved at consumer side | coder-template-sync issue #934, commit 3c12a87a — after fixing envsubst escapes, dry-run surfaced invalid keys `templates/devcontainer/main.tf`, `templates/devcontainer/README.md`, `templates/devcontainer/.terraform.lock.hcl` | 1 | 2026-04-15 | 2026-04-15 |
| Flux adoption of pre-existing kubectl-applied cluster-scoped CR (e.g., RuntimeClass) completes cleanly via SSA — `managedFields[].manager` flips to `kustomize-controller` with no recreation; pilot pods already scheduled on the handler continue running uninterrupted | kata-runtimeclass issue #933, commit 40f20b17 — RuntimeClass `kata` was kubectl-applied for pilot, then added to Flux; adoption verified Ready within ~60s, kata-smoke pod Running undisturbed | 1 | 2026-04-16 | 2026-04-16 |
| New app-template DaemonSet with nodeSelector on label only one node carries (e.g., `kata.spruyt-labs/ready=true`) schedules correctly to single node; DESIRED/CURRENT/READY all 1/1/1; ServiceMonitor + VPA + CNP + Service + SA all created in parallel with HelmRelease, Ready within ~90s | kata-tap-qdisc-fix issue #951, commit b4693597 — scheduled to ms-01-3 only, all aux resources present (VPA `Off` mode with 10Mi recommendation, CNP `kata-tap-qdisc-fix` active) | 1 | 2026-04-16 | 2026-04-16 |
| DaemonSet with `hostNetwork: true` + `hostPID: true` + container `runAsUser: 0` + `runAsNonRoot: false` resolves setns EPERM; pod gets node IP (hostNetwork evidence) and can successfully setns into `/run/netns/cni-*` namespaces; quiet-loop behavior when no target interfaces exist (only startup log; sweep metrics increment silently confirms agent is running) — validate via `/metrics` endpoint counter `*_sweeps_total`, not logs | kata-tap-qdisc-fix issue #951, fix commit 67f55b1d — sweeps_total=5 after ~150s, replace_failures_total=0, no EPERM errors; pod IP 192.168.20.16 == ms-01-3 node IP | 1 | 2026-04-16 | 2026-04-16 |
| `stat /proc/1/ns/net: permission denied` startup crash on hostPID pod with `runAsUser: 0` + CAP_SYS_ADMIN + seccomp RuntimeDefault | containerd's default masked/readonly procfs paths (applied under RuntimeDefault seccomp + no `procMount: Unmasked`) restrict stat of `/proc/<other-pid>/ns/*` even with hostPID, root, and SYS_ADMIN; the syscall needs unmasked procfs | Use `/proc/self/ns/net` instead (works under hostNetwork because self-netns == host-netns), OR set `spec.containers[].securityContext.procMount: Unmasked` (requires feature gate + PSA `privileged`) | 1 | 2026-04-16 | 2026-04-16 |
| Functional smoke test passing on a pod created BEFORE a new DaemonSet version rolled out is a false positive — the qdisc replacement happened under the prior version; new version must be validated for NEW kata pods, not pre-existing ones | kata-tap-qdisc-fix v0.2.0 rollout issue #951 — kata-smoke created 21:48Z, v0.2.0 rolled at 22:33Z, ping still worked but v0.2.0 pod was in CrashLoopBackOff and had never completed a sweep | Compare pod.creationTimestamp against DaemonSet pod startTime; verify metrics counters (*_sweeps_total > 0) before declaring success | 1 | 2026-04-16 | 2026-04-16 |
| Fresh kata-smoke pod on ms-01-3 cannot ARP-resolve its default gateway (10.244.4.222) — ping fails with "Destination Host Unreachable" from pod's own IP even when host-side tap0_kata has correct qdisc (fq) and kata-tap-qdisc-fix daemon is healthy and sweeping | kata-tap-qdisc-fix v0.2.1 validation issue #951 — daemon healthy (sweeps_total ticking 5→7, replacements_total=0 because tap already had fq, replace_failures_total=0); non-kata pod on same node pings coredns fine; ARP for 10.244.4.222 FAILED inside kata guest; veth eth0 inside guest has qdisc noqueue | This is a separate kata-on-cilium networking gap NOT caused by the qdisc daemon — veth/ARP/L2 path between kata VM and cilium host stack needs investigation; do not block tap-qdisc-fix rollout on this | 1 | 2026-04-16 | 2026-04-16 |
| v0.2.1 `/proc/self/ns/net` fix verified: distroless pod starts cleanly with no EACCES/EPERM, sweeps_total increments on 30s cadence, tap0_kata in kata VM netns already carries `qdisc fq` so replacements_total stays 0 in steady state (replacement only happens when qdisc is wrong) | kata-tap-qdisc-fix v0.2.1 rollout issue #951, commit 9fe2f58a (values bump) + b08499ff (code fix) | 1 | 2026-04-16 | 2026-04-16 |
| Terraform template change to coder-template-sync (main.tf) triggers coder-template-push Job to re-run via Flux reconcile; Job image pull ~32s + terraform init/plan ~10s + template push <1s = total ~45s; logs include "Updated version at <timestamp>!" and "Pushed: 1" confirming registration; resource count change in template preview validates spec changes (e.g., dockerd sidecar removed → 3 resources instead of 4); template-only changes never touch the coder server pod or existing workspaces | coder-template-sync issue #933, commit 7a7b2eac — Kata runtime_class_name + node_selector added, dockerd startup block removed; Job completed in ~45s, template preview shows home PVC + workspaces PVC + main pod only | 1 | 2026-04-17 | 2026-04-17 |
| external-secrets webhook replicaCount 1→2 + PDB minAvailable=1 eliminates endpoint gap during Helm rollout; scale-up event followed by clean Helm upgrade; consumer ExternalSecret patches succeed immediately with no TLS webhook errors | external-secrets HA HR values change issue #964, commit 6a045750 — PDB `external-secrets-webhook-pdb` created, currentHealthy=2/desiredHealthy=1, disruptionsAllowed=1 | 1 | 2026-04-17 | 2026-04-17 |
| github-token-rotation force_sync_consumers retry + non-fatal pattern: ATTEMPT/MAX_RETRIES loop around `kubectl patch externalsecret`, failures counted but exit 0; source secret already updated by prior step so best-effort sync is safe | github-token-rotation harden issue #964, commit 6a045750 — manual test job patched 5 ExternalSecrets cleanly (2× claude-agents-write + 2× claude-agents-read + 1× github-mcp) | 1 | 2026-04-17 | 2026-04-17 |
| New HelmRelease with wrong `storageClass` name blocks entire app rollout: PVC stays Pending with ProvisioningFailed (`storageclass.storage.k8s.io "X" not found`), StatefulSet never creates pods, Helm install fails after ~60s with `persistentvolumeclaims "X" not found`, HR enters 5m retry backoff; cluster StorageClass list should be verified against values.yaml before deploy | nexus-system/nexus initial deploy issue #968, commit bae8cfac — specified `rook-ceph-block` but actual SC is `ceph-block`; Flux KS stuck on `Running health checks` for the 15m timeout window | 1 | 2026-04-17 | 2026-04-17 |
